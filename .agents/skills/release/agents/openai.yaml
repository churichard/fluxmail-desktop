interface:
  display_name: "Release"
  short_description: "Validate, build, publish, and repair releases"
  default_prompt: "Use $release to choose the next Fluxmail Desktop version, validate it, build it, and publish it."
