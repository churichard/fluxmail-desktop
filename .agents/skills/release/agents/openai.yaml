interface:
  display_name: "Release"
  short_description: "Validate, build, publish, and repair releases"
  default_prompt: "Use $release to validate, build, and publish the Fluxmail version in package.json."
