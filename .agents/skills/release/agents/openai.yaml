interface:
  display_name: "Release"
  short_description: "Prepare, approve, publish, and repair releases"
  default_prompt: "Use $release to choose the next Fluxmail Desktop version from user-visible changes, validate it, and request approval with the complete changelog before publishing."
